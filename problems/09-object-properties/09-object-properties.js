/* 9. Object Properties
Define function objectProperties(someObj)
Print every property and it's value. */
function objectProperties(someObj) {
    console.log(someObj);
}

let spaceShip = {
    name : 'Millenium Falcon',
    maxSpeed : 'can do kessel run in 12 parcecs',
    weight : 'heavy',
    passengerLoad : 20
};

objectProperties(spaceShip);